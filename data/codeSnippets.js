window.TypeForgeData = window.TypeForgeData || {};
window.TypeForgeData.codeSnippets = [
  'const users = await database.findAll();',
  'SELECT * FROM employees WHERE salary > 5000;',
  'docker compose up --build',
  'function forgeScore(wpm, accuracy) { return wpm * (accuracy / 100); }',
  'npm run dev -- --host 0.0.0.0',
  'git checkout -b feature/typeforge-ui',
  'const palette = { accent: "#6366F1", success: "#22C55E" };',
  'UPDATE employees SET role = "designer" WHERE id = 42;',
  'ls -la && find . -maxdepth 2 -type f | head',
  'const metrics = { wpm: 96, accuracy: 98, mistakes: 4 };'
];
